# MCP AI Summary Demo - MVP 架構學習範例

這是一個學習 MVP (Model-View-Presenter) 架構的範例項目，透過簡單的 AI 摘要生成功能，展示 MVP 架構的基本概念。

## 📚 MVP 架構說明

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│    View     │◄───►│  Presenter   │◄───►│    Model    │
│  (視圖層)   │     │  (展示層)    │     │  (資料層)   │
└─────────────┘     └──────────────┘     └─────────────┘
```

### 各層職責

- **Model (資料層)** - `src/model.py`
  - 資料的存儲和管理
  - 業務邏輯的實現
  - AI 摘要生成服務

- **View (視圖層)** - `src/view.py`
  - 顯示資料給用戶
  - 接收用戶輸入
  - 命令行介面實現

- **Presenter (展示層)** - `src/presenter.py`
  - 接收來自 View 的用戶操作
  - 調用 Model 處理業務邏輯
  - 更新 View 顯示結果

## 🚀 快速開始

### 運行應用

```bash
python main.py
```

### 運行測試

```bash
python -m unittest discover -s tests -v
```

## 📁 項目結構

```
MCP_AI_Summary_demo/
├── main.py              # 主程式入口
├── src/
│   ├── __init__.py
│   ├── model.py         # Model 層 - 資料和業務邏輯
│   ├── view.py          # View 層 - 用戶介面
│   └── presenter.py     # Presenter 層 - 控制器
├── tests/
│   ├── __init__.py
│   └── test_mvp.py      # 單元測試
└── README.md
```

## 🎯 功能特點

1. **生成 AI 摘要** - 輸入文本，生成簡化摘要
2. **查看歷史記錄** - 瀏覽所有生成過的摘要
3. **清除歷史** - 刪除所有摘要記錄
4. **統計資訊** - 顯示摘要字數、壓縮比率等

## 💡 學習重點

- 理解 MVP 架構的分層概念
- 學習如何分離關注點 (Separation of Concerns)
- 掌握介面設計和依賴注入
- 了解單元測試的編寫方式

## 📖 範例使用

```python
from src.view import ConsoleSummaryView
from src.presenter import SummaryPresenter

# 創建 View
view = ConsoleSummaryView()

# 創建 Presenter
presenter = SummaryPresenter(view)

# 生成摘要
presenter.generate_summary("這是一段很長的文本...")
```

## 🔧 技術需求

- Python 3.7+
- 無需額外依賴